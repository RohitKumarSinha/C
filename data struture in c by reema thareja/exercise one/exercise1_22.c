// write a program to print all prime numbers from m to n

#include <stdio.h>

int main()
{
  int from, to;

  printf("enter from where you want the prime numbers :- ");
  scanf("%d", &from);

  printf("enter where up to you want the prime numbers :- ");
  scanf("%d", &to);

  for (from; from <= to; from++)
  {
    for (int i = 2; i <= 10; i++)
     {
       if (from % i == 0 )


     if (i == 11)
        printf()
  }

  return 0;

}
