// write a program to reverse a string using recursion

#include <stdio.h>

int main()
{
  char string[50], ch;

  printf("enter the string :- ");
  gets(string);
  ch = reverse(string);
  printf("%c", ch);
}

char reverse(char string[])
{
  if string
}
