// write a program to check whether a pattern exits in a text. if it does, delete the pattern and display it.

#include <stdio.h>

int main()
{
  char text[100], pattern[20];

  printf("Enter a text :- \n");
  gets(text);

  printf("Enter a pattern you want to delete in text :- ");
  gets(pattern);

  while (text[i] != '\0')
  {
    if (pattern[j] == text[i])
    {
      while (pattern[j] != '\0')
      {
        text[i] =
      }
    }
  }
}
