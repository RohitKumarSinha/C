// write a program to read a line until a newline is entered

#include <stdio.h>

int main()
{
  char str[100];

  printf("write until a newline is entered :- ");
  gets(str);

  return 0;
}
